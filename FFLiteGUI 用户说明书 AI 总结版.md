# FFLiteGUI 用户说明书

## 1. 概述

**FFLiteGUI** 是一个基于 FFmpeg 的图形化多功能工具，旨在提供简洁、高效的音视频处理体验。它集成了转码、滤镜处理、封装合并、画中画、水印、任务队列等丰富功能，适合从新手到专业用户的广泛需求。

### 1.1 主要功能

- **视频转码**：支持多种软件/硬件编码器（x264/x265/NVENC/QSV/AMF/VAAPI/VideoToolbox等），提供 CRF、CQ、固定比特率等多种码率控制方式。
- **视频滤镜链**：裁剪、缩放、旋转/翻转、反交错、降噪、锐化、色彩校正、IVTC、去块、像素格式转换等。
- **截取与分段拼接**：支持精准到帧的截取、组合跳转加速、多片段混剪（带翻转）。
- **音频处理**：独立音频编码、音量调整、变速、倒放、截取、混合（amix）。
- **封装/合并**：流复制或重新编码模式，支持多音轨、多字幕、章节处理。
- **画中画（PIP）**：叠加多个视频/图片，支持独立位置、大小、绿幕抠像、循环、透明度。
- **水印功能**：图片或视频水印，支持自适应缩放、位置可视化编辑。
- **任务队列**：支持并行转码，硬件编码并发控制，任务编辑与导出脚本。
- **预览功能**：基于 mpv 或 ffplay 实时预览滤镜效果（含占位框）。

### 1.2 依赖项

程序依赖于以下外部组件，请确保已正确安装：

- **FFmpeg**：核心编码/解码引擎（必须包含 ffmpeg.exe、ffplay.exe、ffprobe.exe）。
- **mpv**（可选）：推荐作为预览播放器，支持进度条、拖拽等交互。
- **tkinterdnd2**（可选）：用于拖拽文件支持，若未安装仅不影响核心功能。

> 提示：推荐将 FFmpeg 的三个可执行文件放在程序所在目录（绿色便携），或将其所在目录添加到系统 PATH 环境变量。

---

## 2. 安装与启动

### 2.1 下载与解压

从发布页面下载 FFLiteGUI 的压缩包（或源码），解压到任意目录。推荐直接运行 `FFLiteGUI.exe`（Windows）或 `python main.py`（需安装 Python 环境）。

### 2.2 首次运行

- 程序会自动在用户目录（`%USERPROFILE%\.FFLiteGUI`）下创建预设文件 `ffmpeg_presets.json`。
- 若 `ffmpeg_presets.json` 不存在，程序会尝试从 `_internal` 或 `script_dir` 释放默认预设。
- 首次启动时，若检测到 FFmpeg 组件缺失，会在信息日志中显示下载建议。

### 2.3 FFmpeg 配置

程序默认按以下顺序查找 FFmpeg：
1. 自定义目录（在“信息与播放器”标签页设置）
2. 程序所在目录
3. 系统 PATH 环境变量

推荐将 FFmpeg 的 `bin` 目录中的所有 .exe 文件复制到程序目录，以实现便携运行。

---

## 3. 主界面布局

主界面分为左右两大区域：

- **左侧**：包含标签页（视频转码、封装/合并/画中画、信息与播放器）和任务队列。
- **右侧**：上下两个日志窗口，分别显示“关键信息”和“转换进程信息”。

### 3.1 左侧功能区

- **标签页切换**：通过顶部笔记本控件切换不同功能模块。
- **任务队列**：位于转码标签页下方，显示已添加的任务列表，支持双击编辑。

### 3.2 右侧日志区

- **关键信息**：显示操作提示、预设加载、任务状态等。
- **转换进程信息**：显示 FFmpeg 的运行输出（进度、错误等），便于调试。

---

## 4. 视频转码标签页

这是最核心的功能页面，用于设置输入文件、输出路径、编码参数、滤镜等，并支持将当前设置添加到任务队列。

### 4.1 输入/输出设置

#### 输入文件
- 点击“浏览”选择视频文件，或直接拖拽文件到输入框（需安装 tkinterdnd2）。
- 拖拽文件夹会递归扫描其中的视频文件（扩展名为 `.mp4`, `.mkv`, `.avi` 等）。

#### 输出目录
- 默认与输入文件同目录，可自定义。
- 支持拖拽文件夹到输出框自动填充。

#### 文件名控制
- **后缀**：在输出文件名后添加自定义后缀（如 `_new`）。
- **完整自定义名称**：若填写，将覆盖后缀和容器扩展名。
- **输出容器**：选择封装格式（mp4/mkv/mov/avi/webm/gif/webp）。

> 注：选择 gif 或 webp 时，程序会自动调整编码器和扩展名。

### 4.2 参数预设管理

预设可以保存当前所有转码设置（除水印和分段拼接外），方便快速切换。

- **预设下拉框**：选择已保存的预设，加载到界面。
- **保存当前参数为预设**：弹出对话框输入预设名称，保存。
- **删除预设**：删除选中的预设。
- **导出所有预设(备份)**：将整个预设库导出为 JSON 文件。
- **导入预设(恢复)**：从 JSON 文件导入预设（可合并或替换）。

> 预设文件位置：默认在用户目录 `%USERPROFILE%\.FFLiteGUI\ffmpeg_presets.json`。

### 4.3 视频编码与质量

此面板控制视频编码的核心参数。

#### 编码器选择
- **流复制 (copy)**：直接复制视频流，不重新编码（速度最快，但滤镜和帧率等设置将被忽略）。
- **软件编码**：`libx264`, `libx265`, `libvpx-vp9`, `libsvtav1` 等，兼容性好，画质优。
- **硬件编码**：
  - NVIDIA NVENC：`h264_nvenc`, `hevc_nvenc`, `av1_nvenc`
  - Intel QSV：`h264_qsv`, `hevc_qsv`, `av1_qsv`
  - AMD AMF：`h264_amf`, `hevc_amf`, `av1_amf`
  - VAAPI（Linux）, VideoToolbox（macOS）
- **专业格式**：`prores_ks`, `dnxhdenc`, `ffv1` 等。
- **动图格式**：`gif`, `libwebp`（选择后自动隐藏高级选项，显示 GIF 选项按钮）。

#### 编码预设
控制速度与压缩率的平衡：
- 软件编码：`ultrafast` → `veryslow`（速度递减，压缩率递增）
- NVENC：`p1` ~ `p7`（p1 最快，p7 画质最好）
- QSV：`veryfast` ~ `slower`
- AMF：`quality`/`speed`/`balanced`

#### 码率控制
- **CRF**（软件编码）：值 0~51，越小质量越好（推荐 18~28）。
- **CQ**（NVENC）：值 0~51，类似 CRF。
- **Global Quality**（QSV）：值 1~51。
- **固定比特率**：指定目标码率（如 1900k），适合流媒体场景。

#### 高级参数（展开面板）
- **tune**：针对特定内容优化（film/animation/grain/psnr/ssim/fastdecode/zerolatency/vmaf/screen）。
- **profile**：编码配置文件（如 H.264 的 baseline/main/high）。
- **level**：编码等级（限制码率/分辨率）。
- **最大比特率**（maxrate）和 **缓冲区大小**（bufsize）：用于约束峰值码率。

#### GIF 输出选项
仅当编码器为 `gif` 时显示：
- **循环次数**：0 表示无限循环。
- **抖动算法**：none/bayer/floyd_steinberg/sierra2_4a。
- **Bayer Scale**：当抖动为 bayer 时，控制抖动强度。
- **调色板大小**：max_colors（2~256）。

### 4.4 视频滤镜

所有滤镜将按顺序应用到视频流。

#### 帧率
- **保持源**：不改变帧率。
- **指定**：自定义帧率（如 30 fps），可输入常用值。

#### 字幕烧录
- 勾选后，选择字幕文件（.srt/.ass/.ssa/.vtt），将字幕永久嵌入画面（需重新编码）。
- 若希望保留独立字幕轨道，请在“封装/合并”标签页添加外部字幕。

#### 缩放
- **宽度（高度自动）**：指定宽度，高度按比例计算。
- **高度（宽度自动）**：指定高度，宽度按比例计算。
- **精确宽×高**：自定义宽度和高度，可能拉伸画面。

#### 裁剪
- 支持表达式：`iw`（原宽），`ih`（原高），可参与运算，如 `iw/2`。
- **自动去黑边**：分析视频前几帧，自动检测黑边并填入裁剪参数。
  - 可调整“分析帧数”和“round”（宽高对齐值）。
  - 检测将从当前截取起始时间开始（若有），避免片头黑屏干扰。
- **可视化裁剪**：打开窗口，显示视频首帧，可拖拽绘制矩形选区，参数自动回填。
  - 支持跳转到指定时间重新取帧（便于从特定帧裁剪）。

#### 旋转/翻转
- 旋转：90°/180°/270°（实体旋转，非元数据）。
- 上下翻转 / 左右翻转。

#### 高级增强（独立窗口）
- **降噪 (hqdn3d)**：空间/时间强度（0~10）。
- **锐化 (unsharp)**：强度（0~5）。
- **IVTC（反胶卷过带）**：将 60i 转换为 24p（预览可能失败，请谨慎启用）。
- **去块滤波 (deblock)**：强度 1~8。
- **色彩空间转换**：矩阵如 `bt709:bt2020`。
- **颜色校正**：亮度、对比度、饱和度、伽马。
- **色相调整**：色相角度和色饱和度。

#### 反交错
- 选项：bwdif, yadif, kerndeint, pp=lb, fieldorder 等。
- 若已启用 IVTC，反交错将被忽略（IVTC 自带反交错）。

#### 像素格式
- 常用：yuv420p（最兼容），yuv422p, yuv444p, yuv420p10le（HDR）, p010le（硬件友好）等。

#### 变速
- 启用后输入速度倍数（如 0.5 慢放，2.0 快放）。
- 音频会自动同步变速（通过 atempo 滤镜，支持大于 2 倍或小于 0.5 倍的串联）。

#### 倒放
- 勾选后视频将倒放（反向播放），音频也会同步倒放。

### 4.5 音频

#### 基本控制
- **保留音频**：是否输出音频流。
- **仅提取音频**：只输出音频文件（自动添加 `-vn`），容器格式自动切换为音频格式（如 m4a）。

#### 编码参数
- 编码器：aac, mp3, opus, ac3, flac, alac, copy 等。
- 比特率：如 128k。
- 采样率：如 44100 Hz。

#### 音量调整
- 启用后拖动滑块调整倍数（0.1~3.0）。

### 4.6 截取片段

控制输出视频的时间范围。

- **启用截取**：勾选后设置开始/结束时间。
  - 格式：`HH:MM:SS[.mmm]` 或纯秒数（如 10.5）。
  - 结束时间留空表示到文件末尾。
- **精准到帧**：勾选后截取精确到帧（必须重新编码）。
  - 适用于需要精确剪辑的场景（如与水印/画中画配合）。
- **组合跳转**（仅显示于主转码页面）：加速长视频的精确截取。
  - 原理：先通过 `-ss` 快速跳转到目标附近（前置），再用后置 `-ss` 精确微调。
  - 适用条件：单个视频、未启用水印/画中画、编码器非 copy、起始时间 > 阈值。
  - 阈值默认为 30 秒，通常无需修改。

> 注意：组合跳转与精准模式互斥，且在水印/画中画模式下自动禁用。

### 4.7 分段拼接（简单混剪）

此功能允许将同一视频的不同片段裁剪后拼接成一个新视频，适合去中间的广告、提取精彩片段等。

- **启用分段拼接模式**：勾选后，将忽略“截取片段”设置，使用下方片段列表。
- **打开分段设置**：弹出窗口管理片段列表。

#### 片段列表管理
- **添加片段**：手动输入开始/结束时间，可选择翻转（水平/垂直）。
- **删除/上移/下移**：调整片段顺序。
- **双击片段**：编辑时间或翻转。
- **清空所有**：移除全部片段。

#### 外部命令导入
- 在右侧文本框粘贴 FFmpeg 截取命令（每行一条），程序自动解析 `-ss` 和 `-t`/`-to` 参数，生成片段。
  - 支持双 `-ss` 组合跳转格式（取最后一个 `-ss`）。
  - 忽略以 `#` 开头的注释行。
- 点击“解析并导入所有片段”。

#### 导出分段脚本 / 发送到队列
- **快速**：使用流复制（`-c copy`）截取，速度快但可能不精确（基于关键帧）。
- **精确**：应用主界面全部滤镜设置，重新编码，帧级精准。
  - 会弹出选择框，询问使用“双 -ss”还是“trim 滤镜”实现精确截取。
- 可导出为 `.bat`/`.sh` 脚本，或直接添加到任务队列。

### 4.8 高级选项

#### 硬件解码
- 启用后可加速解码过程，尤其适合高分辨率视频。
- 常用选项：
  - `cuda`（NVIDIA 通用，自动识别格式）
  - `qsv`（Intel）
  - `vaapi`（Linux）
  - 专用解码器如 `h264_cuvid`（手动指定）
- 推荐 NVIDIA 用户使用 `cuda`，Intel 用户使用 `qsv`。

#### 自定义 FFmpeg 参数
- 在文本框输入额外参数，将追加到命令末尾（会覆盖界面生成的对应设置）。
- 可用于添加 `-x264-params`、`-bsf` 等高级选项。
- 注意：若添加 `-vf` 或 `-filter_complex`，会覆盖界面生成的滤镜链，请谨慎使用，如需修改请复制预览区生成的滤镜链追加自己的滤镜，最后完整粘贴到自定义。

#### 水印设置
- **水印文件**：选择图片或视频文件作为水印。
- **自适应**：勾选后，水印的尺寸和位置会根据主视频尺寸自动缩放（需设置基准尺寸）。
- **水印叠加设置**：打开独立窗口，可配置水印的滤镜、位置、绿幕、循环、透明度等（详见 5.5 节）。
- **水印预设**：可保存常用水印参数为模板，快速加载。

> 提示：水印模式会强制启用精准截取，并自动禁用组合跳转。

---


## 5. 封装/合并/画中画标签页

此标签页提供高级的流处理能力，包括多轨道封装、视频串行合并、画中画叠加等。它允许您将多个视频、音频、字幕文件组合成一个输出文件，同时支持独立调整每个轨道的编码参数。

### 5.1 基本概念

在封装/合并标签页中，您需要理解以下概念：

- **主视频**：作为输出视频的“画布”或“基准”文件。通常第一个启用的视频轨道被视为主视频。
- **轨道（Track）**：每个输入文件可以包含多个流（视频、音频、字幕）。程序会将每个流作为一个独立的轨道列出。
- **轨道列表**：显示当前已加载的所有轨道，包括启用状态、类型、编码、来源文件等信息。
- **输出容器**：最终封装格式（mkv/mp4/webm）。不同容器对流格式有不同的兼容性要求。

### 5.2 主视频与轨道管理

#### 主视频文件
- 点击“浏览”选择主视频文件。程序会自动解析其中的视频、音频、字幕流，并填充到轨道列表。
- 主视频在轨道列表中显示为“视频(主)”，且其视频流是输出的基础。

#### 轨道列表操作
- **启用/禁用**：通过每行左侧的复选框控制该轨道是否参与输出。
- **编码设置**：点击“编码设置”按钮，可单独调整该轨道的编码参数（视频/音频/字幕各有不同设置页）。
- **预览**：点击“预览”按钮，可单独预览该轨道的内容（支持滤镜预览）。
- **上移/下移**：调整轨道在输出中的顺序（影响流映射顺序）。
- **删除**：从列表中移除该轨道。

#### 添加外部轨道
- **添加外部音轨**：选择音频文件（mp3/aac/wav/flac等），将其音频流添加为独立音轨。
- **添加外部字幕**：选择字幕文件（srt/ass/ssa/vtt等），添加为独立字幕轨。
- **添加外部视频（画中画/串行）**：根据当前模式（画中画或串行合并）添加视频文件。
- **清空轨道**：移除所有非主视频的轨道。

### 5.3 三种工作模式

封装/合并标签页支持三种互斥的工作模式：

#### 5.3.1 普通封装模式（默认）

**适用场景**：将一个主视频与多个外部音频/字幕流封装在一起，不改变视频内容。

- **特点**：
  - 主视频的视频流作为输出视频
  - 可添加多音轨（支持语言标记）
  - 可添加多字幕轨（支持标题和语言）
  - 支持章节复制或外部章节文件导入
  - 音频混合（amix）可将多个音轨合并为单音轨

- **操作流程**：
  1. 选择主视频文件（自动加载其所有流）
  2. 添加外部音轨或字幕（或拖拽文件）
  3. 根据需要调整各轨道的编码设置
  4. 选择输出容器，生成输出路径
  5. 点击“开始合并”

#### 5.3.2 串行合并模式（首尾拼接）

**适用场景**：将多个视频文件按顺序首尾拼接成一个长视频，适合合并剧集、连续片段等。

- **启用方式**：勾选“串行合并（首尾拼接）”复选框（与画中画互斥）。

- **两种实现方式**：

  | 模式 | 条件 | 特点 |
  |------|------|------|
  | **流复制模式** | 所有视频编码参数完全一致（分辨率、帧率、像素格式等） | 速度极快，不重新编码，无损拼接 |
  | **重新编码模式** | 任意条件不满足 | 统一编码参数，兼容性好，耗时较长 |

  > **重要提示**：流复制模式要求所有源文件的编码参数完全一致。若不一致，可能导致拼接处播放速度异常、音画不同步或花屏。建议不确定时使用重新编码模式。

- **音频绑定**（仅重新编码模式）：
  - **使用视频自身音频**：每个视频片段使用其自带的音频流。
  - **生成静音流**：为没有音频的视频片段生成静音流，避免音画错位。

#### 5.3.3 画中画模式（PIP）

**适用场景**：将多个视频/图片叠加到主画面上，适合多机位合成、画中画解说、角标水印等。

- **启用方式**：勾选“启用画中画”复选框（与串行合并互斥）。

- **特性**：
  - 所有视频流强制重新编码（无法使用 copy）
  - 每个子视频可独立设置位置、大小、绿幕抠像、透明度、循环等
  - 输出时长由主视频决定（或手动指定）
  - 音频流可独立控制是否倒放

- **子视频的设置**：
  - 点击轨道列表中的“编码设置”按钮，进入视频轨道设置窗口
  - 在“叠加/偏移”页面调整位置（支持可视化编辑）
  - 在“循环/绿幕控制”页面设置循环播放和抠像

### 5.4 轨道编码设置详解

#### 视频轨道设置

点击视频轨道的“编码设置”按钮，打开独立设置窗口，包含以下页面：

1. **编码器与质量**：
   - 与主转码页面相同的编码参数（编码器、预设、码率控制等）
   - 注意：画中画模式下，从视频的编码器会被忽略，统一使用主视频的编码器

2. **视频滤镜**：
   - 支持裁剪、缩放、旋转/翻转、反交错、降噪、锐化、色彩校正等
   - 与主转码页面的滤镜功能完全相同

3. **截取片段**：
   - 可单独截取该视频的一部分用于叠加
   - 画中画模式下自动启用精准截取

4. **循环/绿幕控制**（仅子视频）：
   - **循环播放**：控制子视频的重复播放次数（无限/自定义次数/仅一次）
   - **绿幕抠像**：支持 chromakey 或 colorkey 两种算法
     - 颜色选择：预置色值或吸管取色（Windows）
     - 相似度：0~1，控制抠像的敏感度
     - 混合度：0~1，控制边缘平滑程度
   - **透明度**：整体透明度控制（0~1）

5. **叠加/偏移**：
   - **位置控制**：X/Y 坐标（支持表达式如 `W-w-10`）
   - **快速预设**：左上角/右上角/左下角/右下角/居中
   - **可视化编辑**：打开交互式窗口，拖拽调整位置和大小

#### 音频轨道设置

点击音频轨道的“编码设置”按钮，可调整：

- **编码参数**：编码器、比特率、采样率
- **音量调整**：启用后拖动滑块（0.1~3.0倍）
- **音频混合**（普通封装模式）：勾选后该流参与 amix 混合，输出为单音轨
- **音频倒放**（画中画模式）：独立控制该音频流是否倒放
- **音频截取**：精确到毫秒的时间截取（启用后编码器自动改为非 copy）

#### 字幕轨道设置

点击字幕轨道的“编码设置”按钮，可调整：

- **编码器**：copy（保留原始格式）、mov_text（MP4兼容）、srt
- **语言代码**：如 chi（中文）、eng（英语）
- **轨道标题**：自定义显示名称

> 注意：MP4 容器对 ASS/SSA 字幕支持不佳，推荐使用 MKV 容器并选择 copy 流。

### 5.5 水印设置（独立于轨道）

在“视频转码”标签页的“高级选项”面板中，提供了独立的水印功能，与画中画模式不同，它直接集成在主转码流程中。

#### 水印文件选择
- 支持图片（png/jpg/bmp/webp）和视频（mp4/mkv/avi等）
- 视频水印可通过截取和循环控制播放次数

#### 水印叠加设置（独立窗口）
打开后包含完整的滤镜设置页：
- **编码器与质量**：水印自身的编码参数（仅视频水印有效）
- **视频滤镜**：裁剪、缩放、旋转等（影响水印的最终渲染尺寸）
- **截取片段**：截取水印的一部分用于叠加
- **循环/绿幕控制**：与画中画子视频相同
- **叠加/偏移**：位置控制 + 可视化编辑

#### 自适应功能
- 勾选“自适应”后，水印的位置和尺寸会根据主视频尺寸自动缩放
- 首次使用时，会以当前主视频尺寸作为基准
- 当主视频尺寸变化时，水印参数会自动按比例调整

#### 水印预设管理
- 可保存当前水印设置为模板（不含文件路径）
- 快速加载已保存的水印配置

### 5.6 章节处理

章节信息可以来自源文件或外部文件：

- **从源文件复制章节**：勾选后，将从第一个输入文件（主视频）复制章节信息。
- **导入外部章节文件**：选择 FFmetadata 格式的章节文件（通常为 .txt 格式）。
  - 格式示例：
    ```
    ;FFMETADATA1
    [CHAPTER]
    TIMEBASE=1/1000
    START=0
    END=10000
    title=Chapter 1
    ```

> 若同时勾选“复制章节”和导入外部文件，外部文件优先级更高。

### 5.7 输出与合并执行

#### 输出设置
- **输出容器**：mkv / mp4 / webm（推荐 MKV 以获得最佳字幕兼容性）
- **输出文件**：自动生成路径（可手动修改或浏览选择）

#### 手动时长控制
- 勾选“手动时长”后，可指定输出总时长（秒），覆盖自动计算的时长
- 主要用途：
  1. **应急保险**：防止因滤镜循环或参数不当导致输出无限延长
  2. **快速测试**：输出 10 秒片段查看效果

#### 开始合并
点击“开始合并”按钮后：
1. 程序检查轨道和设置的有效性
2. 生成最终命令并执行
3. 输出文件生成后，可选择是否删除源文件（需确认）
4. 执行过程显示在右侧日志区，进度在标题栏更新

#### 验证输出
- 勾选“验证输出文件”后，合并完成后会检查输出文件是否存在且大小 > 0。
- 若验证失败，源文件不会被删除。

---

## 6. 信息与播放器标签页

此标签页集中管理播放器设置、日志记录、FFmpeg 路径配置，并提供快速命令工具和系统状态检测。

### 6.1 播放器设置

#### mpv 预览
- **启用 mpv 作为预览播放器**：推荐开启，mpv 支持进度条拖拽、播放控制等交互体验。
- **mpv 可执行文件路径**：
  - 默认值为 `mpv`（要求在 PATH 中）
  - 可浏览选择自定义路径（如 `C:\mpv\mpv.exe`）
  - mpv 下载地址：https://mpv.io/installation/

> 未启用 mpv 时，使用 ffplay 预览（功能有限，不支持进度条）。

#### 日志记录
- **记录成功命令到日志**：勾选后，所有成功的转码/合并命令会追加到日志文件。
- **日志文件路径**：默认为程序目录下的 `editlog.txt`，可自定义。

#### FFmpeg 自定义目录
- 勾选“启用自定义 FFmpeg 目录”后，程序将使用指定目录下的 ffmpeg.exe、ffprobe.exe、ffplay.exe。
- 适用于需要切换 FFmpeg 版本以支持特定硬件编码的场景。

### 6.2 快速命令工具

这是一个高级功能，允许您直接输入或选择预设的 FFmpeg 命令并执行，适合需要快速测试或批量处理的高级用户。

#### 预设命令选择
- 下拉列表包含内置的常用命令模板（如生成测试视频、提取关键帧、音频降噪等）。
- 选择后自动填充到命令编辑框，占位符 `{input}` 和 `{output_dir}` 会被替换为当前设置值。

#### 命令编辑框
- 可手动输入或修改 FFmpeg 命令。
- 支持多行输入，但一次执行仅运行一个命令。

#### 输出目录
- 指定命令执行时的工作目录（生成的文件将保存到此目录）。
- 默认为当前工作目录。

#### 操作按钮
- **运行命令**：在独立线程中执行命令，输出显示在日志区域。
  - 支持全局“停止所有转码”功能（发送 `q` 信号）。
- **复制到剪贴板**：复制编辑框中的命令。
- **获取**：从转换预览区或合并预览区获取当前生成的命令。
- **重载**：重新加载命令模板文件（修改 `quick_cmds.json` 后使用）。
- **清空**：清空命令编辑框。

#### 自定义模板
- 模板文件位置：与主预设同目录下的 `quick_cmds.json`。
- 格式：`{"显示名称": "命令模板"}`
- 支持占位符：`{input}`（输入文件路径）、`{output_dir}`（输出目录）。

### 6.3 状态检测

此区域显示当前系统状态，帮助诊断配置问题：

- **预设配置文件**：显示预设文件路径和状态（存在/不存在）。
- **mpv/ffplay 状态**：显示预览播放器的可用性。
- **FFmpeg 全家桶路径**：显示实际使用的 ffmpeg、ffprobe、ffplay 路径及来源（自定义/系统PATH）。
- **环境变量 PATH 检测**：列出在 PATH 中找到的 FFmpeg 工具。
- **打开预设文件夹**：在文件管理器中打开预设所在目录，便于手动编辑配置文件。

---

## 7. 任务队列与批量处理

任务队列是 FFLiteGUI 的核心生产力工具，允许您将多个转码任务加入队列，统一管理并并行执行。

### 7.1 添加任务

#### 通过界面添加
1. 在“视频转码”标签页设置好所有参数。
2. 点击“添加到任务列表”按钮，当前设置会生成一个任务并加入队列。

#### 通过拖拽添加
- 从文件管理器拖拽视频文件到输入框或主窗口，程序会自动按当前设置生成任务。
- 拖拽文件夹会递归扫描其中的视频文件并批量添加。

#### 任务列表显示
任务列表包含以下列：
- **序号**：任务在列表中的顺序。
- **文件名**：输入文件的名称。
- **输出路径**：生成的输出文件完整路径。
- **命令（简洁）**：简化的命令显示（隐藏完整路径，便于查看）。
- **状态**：等待/转码中/完成/失败/已停止（转码中显示进度百分比）。
- **错误信息**：任务失败时的错误信息（截断显示）。

### 7.2 任务管理操作

#### 双击编辑
双击任务行可打开编辑窗口，修改任务的参数设置（与主界面设置类似）。支持编辑等待、失败或已完成的任务。

#### 移除任务
- **移除选中任务**：从列表中移除选中的任务（转码中的任务不可删除）。
- **清空全部任务**：移除所有任务（需先停止队列）。
- **清空已完成/失败任务**：仅移除状态为“完成”或“失败”的任务。

#### 预览选中任务
选中任务后点击“预览选中任务”，使用当前任务的设置预览该文件（不影响任务状态）。

### 7.3 并行队列执行

#### 并行设置
- **并行任务**：同时运行的最大任务数（1~5），推荐不超过 3 以避免资源过度占用。
- **硬编并发限制**：同时进行的硬件编码任务的最大数量（1~4），推荐不超过 2。

#### 硬件编码识别
程序会自动识别使用硬件编码器（NVENC/QSV/AMF/VAAPI/VideoToolbox）的任务，并应用并发限制，避免显存冲突。

#### 队列控制
- **开始队列**：启动队列处理，自动提交等待中的任务。
- **停止队列**：停止提交新任务，但已在运行的任务会继续完成。
- **停止所有转码**：强制停止所有正在运行的 FFmpeg 进程（发送 `q` 信号，3秒后若未退出则强制终止）。

#### 导出脚本
将当前任务列表导出为批处理脚本：
- Windows：`.bat` 格式
- Linux/macOS：`.sh` 格式
- 脚本包含所有任务的完整 FFmpeg 命令

---

## 8. 预览功能详解

FFLiteGUI 提供多种预览方式，帮助您在转码前验证效果。

### 8.1 预览当前命令

在“视频转码”标签页点击“预览当前命令”按钮：
- 使用当前界面的全部设置（包括滤镜、截取、水印、画中画等）预览输入文件。
- 视频会自动缩放以适应屏幕（保留宽高比）。
- 支持音频变速预览（仅 ffplay）。
- 画中画模式下，子视频会显示红色占位框（预览实际位置和大小）。

### 8.2 预览选中任务

在任务列表选中任务后，点击“预览选中任务”：
- 使用该任务保存的设置预览其输入文件。
- 与“预览当前命令”功能相同，但使用任务设置而非当前界面设置。

### 8.3 预览单个轨道

在“封装/合并/画中画”标签页的轨道列表中，点击轨道的“预览”按钮：
- 预览该轨道的内容，应用其独立的滤镜设置。
- 支持视频/音频轨道的预览（字幕轨道不支持预览）。

### 8.4 预览限制

- **倒放**：预览不支持视频/音频倒放（会忽略此设置）。
- **变速**：mpv 预览不支持音频变速（ffplay 支持）。
- **水印/画中画**：预览时会显示红色占位框代替实际内容，便于确认位置和大小。
- **截取**：预览时会通过播放器的 `-ss`/`--start` 参数跳转，而非应用截取滤镜。

---

## 9. 常见问题与技巧

### 9.1 FFmpeg 找不到怎么办？

**现象**：程序提示“未找到 ffmpeg”或预览/转码失败。

**解决方法**：
1. 将 ffmpeg.exe、ffplay.exe、ffprobe.exe 放在程序同一目录（绿色便携）。
2. 或将 FFmpeg 的 bin 目录添加到系统 PATH 环境变量。
3. 在“信息与播放器”标签页中，启用“自定义 FFmpeg 目录”并指定路径。

### 9.2 硬件编码器不可用

**现象**：选择 NVENC/QSV 等硬件编码器后转码失败。

**可能原因**：
- FFmpeg 版本与显卡驱动 API 不兼容（NVIDIA NVENC 需要特定版本）。
- 显卡驱动未安装或版本过旧。
- 系统不支持该硬件编码器（如 AMD 显卡选择了 NVENC）。

**解决方法**：
1. 更新显卡驱动。
2. 尝试更换 FFmpeg 版本（使用 BtbN 的 shared 版本）。
3. 使用软件编码器作为备选。

### 9.3 画中画/水印模式下输出无限延长

**现象**：转码一直持续，文件大小不断增大，无法结束。

**原因**：子视频或水印设置了无限循环（loop=-1），且主视频没有音频流导致 `-shortest` 失效。

**解决方法**：
1. **立即停止**：点击“信息与播放器”标签页的“停止所有转码”按钮。
2. **预防**：
   - 启用“手动时长”控制，设置明确的输出时长。
   - 确保主视频包含音频流（或手动添加静音音频）。
   - 在自定义参数中添加 `-t` 参数。

### 9.4 裁剪参数不生效

**现象**：裁剪设置了但输出画面未裁剪。

**可能原因**：
- 编码器设置为 `copy`（流复制模式下不应用滤镜）。
- 裁剪表达式结果无效（如 `iw/0` 导致错误）。

**解决方法**：
- 将编码器改为非 copy 模式（如 libx265）。
- 检查裁剪表达式是否合法（宽高必须为正数）。

### 9.5 精准截取与水印/画中画冲突

**现象**：启用水印或画中画后，截取时间不准确。

**说明**：水印/画中画模式下，程序会强制启用精准截取（通过 `trim` 滤镜实现），以确保叠加对齐。

**建议**：无需额外操作，程序会自动处理。

### 9.6 音频编码器自动从 copy 改为 aac

**现象**：选择了音频编码器为 copy，但转码时提示改为 aac。

**原因**：
- 应用了音频滤镜（音量、变速、倒放、截取等）。
- 音频混合（amix）模式。
- 音频轨道启用截取。

**说明**：这是正常行为，因为 copy 模式无法同时应用滤镜。

### 9.7 预设加载后部分参数未恢复

**检查项**：
- 水印设置不会被保存到预设（需单独管理水印模板）。
- 分段拼接设置不会被保存。
- 截取设置不会被保存。

**解决方法**：这些参数与具体文件相关，建议单独配置。

### 9.8 快捷键与效率技巧

- **拖拽添加**：将视频文件直接拖到输入框或主窗口，快速添加任务。
- **批量操作**：选中多个任务后，右键可进行批量编辑（仅限支持的操作）。
- **日志保存**：右侧日志区可随时保存为文本文件，便于调试。
- **命令复制**：从预览区复制生成的命令，可粘贴到终端手动执行。

### 9.9 文件路径中的空格处理

程序会自动处理路径中的空格，在显示命令时会添加适当的引号。执行时使用列表形式传递参数，无需担心空格问题。

### 9.10 自定义 FFmpeg 参数的注意事项

- 添加 `-vf` 或 `-filter_complex` 会覆盖界面生成的滤镜链。
- 添加 `-af` 会覆盖界面生成的音频滤镜。
- 添加 `-map` 会覆盖界面生成的流映射。
- 建议仅添加界面未提供的参数（如 `-x264-params`、`-bsf` 等）。

---


## 10. 术语表

| 术语 | 说明 |
|------|------|
| **CRF** | Constant Rate Factor，恒定质量因子。值越小质量越高，文件越大。|
| **CQ** | Constant Quality，恒定质量，类似 CRF，用于 NVENC。|
| **PTS** | Presentation Time Stamp，显示时间戳，用于控制帧的显示顺序。|
| **SAR** | Sample Aspect Ratio，采样纵横比。|
| **IVTC** | Inverse Telecine，反胶卷过带，将 60i 转换为 24p。|
| **BWDIF** | Bob Weaver Deinterlacing Filter，一种反交错算法。|
| **chromakey** | 色度键，基于色相/饱和度抠像。|
| **colorkey** | 颜色键，基于 RGB 距离抠像。|
| **amix** | 音频混合滤镜，将多路音频混合为一路。|
| **concat** | 串联拼接，将多个视频首尾相连。|
| **overlay** | 叠加滤镜，将视频/图片覆盖在另一个视频之上。|
| **PIP** | Picture-in-Picture，画中画。|
| **FFmpeg** | 开源音视频处理工具。|
| **mpv** | 开源媒体播放器，支持 FFmpeg 滤镜。|

---

*FFLiteGUI 用户说明书至此结束。如有未尽事宜，请参考程序内的 ToolTip 提示或查看源代码中的注释。*